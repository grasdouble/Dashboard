git:
  quiet: true

cache:
  directories:
    - node_modules
notifications:
  email: false

matrix:
  fast_finish: true
  include:
    - language: node_js
      env: NAME=test_server
      node_js:
        - '11'
      script:
        - npm test:server

# branch safelist
branches:
  only:
    - develop
    - master
